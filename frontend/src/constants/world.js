/** Borde del mundo en px (compartido entre `/game` y `/game/second`). */
export const WORLD_EDGE = 1200

/** Mitad del ancho del portal de transición (centro en WORLD_EDGE / 2). */
export const PORTAL_HALF_WIDTH = 200
