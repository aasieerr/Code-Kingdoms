/** Borde del mundo en px (compartido entre `/game` y `/game/second`). */
export const WORLD_EDGE = 1200

/** Ancho en px del lobby (`MainView`) y de la arena (`SecondView`). Alineado con mapas / walk bounds 1700×1200. */
export const WORLD_WIDTH = 1700

/** Mitad del ancho del portal de transición (centro en WORLD_WIDTH / 2). */
export const PORTAL_HALF_WIDTH = 200
